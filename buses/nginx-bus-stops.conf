# Nginx location for per-stop JSON files
# Serve /stops/{stopId} -> /var/www/stops/{stopId}.json
# Example: GET /stops/370000123

location /stops/ {
  alias /var/www/stops/;
  default_type application/json;
  add_header Cache-Control "public, max-age=10";
  try_files $uri.json =404;
}
